import {SceneNames} from "./SceneNames";

export class Stack extends Phaser.Scene {
    constructor() {
        super({key: SceneNames.Stack});
    }

    public preload() {
    }

    public create() {
    }
}